// Package e2eplan validates and renders the immutable safety envelope that
// must be reviewed before a real Scaleway end-to-end run. It performs no cloud,
// Kubernetes, filesystem, credential, or process mutation.
package e2eplan
